"""Custom views and voxel grid extensions for the GA-036 analysis.

Extensions on top of ``docktgrid``:
- ``CustomVoxelGrid``: adds ``gaussian``, ``gaussian_amax``, ``vdw2`` and
  ``vdw2_matmul`` occupancy functions.
- ``*View*`` classes: views that separate protein / ligand / complex channels.
"""
